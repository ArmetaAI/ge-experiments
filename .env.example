# ============================================================================
# Local Development Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values
# For local development:
# 1. Start Cloud SQL Proxy: cloud-sql-proxy YOUR_PROJECT:YOUR_REGION:YOUR_INSTANCE --port 5433
# 2. Authenticate: gcloud auth application-default login

PROCESSING_MODE=development

# Google Cloud Project
GOOGLE_CLOUD_PROJECT=your-project-id

# Database Configuration (Local Development via Cloud SQL Proxy)
DATABASE_URL=postgresql+psycopg2://postgres:YOUR_PASSWORD@127.0.0.1:5433/postgres
DB_USER=postgres
DB_PASS=YOUR_PASSWORD
DB_NAME=postgres
INSTANCE_CONNECTION_NAME=your-project:your-region:your-instance

# Google Cloud Storage
GCS_BUCKET_NAME=your-bucket-name
GCS_TEST_BUCKET_NAME=your-test-bucket
GCS_PROJECT_ID=your-project-id

# LangSmith Configuration (Optional)
LANGCHAIN_API_KEY=your_langsmith_api_key_here
LANGCHAIN_TRACING_V2=false
LANGCHAIN_PROJECT=your-project-name

LMNR_PROJECT_API_KEY=""

# ============================================================================
# Production Deployment Notes
# ============================================================================
# In Cloud Run, remove DATABASE_URL from environment variables.
# The app will use INSTANCE_CONNECTION_NAME with Cloud SQL Python Connector
# and automatic IAM authentication.
#
# File Processing:
# - No artificial size limits imposed by application
# - Only infrastructure limits apply (Cloud Run, network, etc.)
# - System optimized for large file processing with streaming
